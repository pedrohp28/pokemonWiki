spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pokemon_wiki?useTimezone=true&serverTimezone=UTC
    username: root
    password: 12345678
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
  security:
      user:
        name: admin
        password: 12345678
springdoc:
  pathsToMatch:
    - /api/**/v1/**
#    - /auth/**
  swagger-ui:
    use-root-path: true